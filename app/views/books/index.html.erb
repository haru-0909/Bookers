<h1>Books</h1>

<% @books.each do |book| %>

<p>title</p>
<span><%= book.title %></span>

<p>body</p>
<span><%= book.body %></span>
<span>
  <%= link_to "show", "/books/#{book.id}" %>
</span>
<span>
  <%= link_to "edit", "/books/#{book.id}/edit" %>
</span>
<%= link_to "destroy", destroy_book_path(book.id), method: :delete, "deta-confirm" => "本当に削除しますか？" %>
<% end %>

<h2>New book</h2>

<%= form_with model: @book, url:'/show', method: :post do |f| %>
  <h4>title</h4>
  <%= f.text_area :body %>

  <h4>body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>